language: node_js

node_js:
- 9.2.1

services:
  - postgresql

addons:
  postgresql: "9.4"

before_script:
  - psql -c 'CREATE DATABASE "maco-bot-test";' -U postgres
  - knex migrate:latest

env:
  - TEST_DIR=./
  - TEST_DIR=./client

script: cd $TEST_DIR && npm install && npm test
